specVersion: "v1"
serviceId: "smartchat"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "smartchat-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
